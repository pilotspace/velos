---
phase: 08-tuning-vehicle-behavior-to-more-realistic-in-hcm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/velos-vehicle/Cargo.toml
  - crates/velos-vehicle/src/lib.rs
  - crates/velos-vehicle/src/config.rs
  - crates/velos-vehicle/src/types.rs
  - crates/velos-vehicle/src/krauss.rs
  - crates/velos-vehicle/src/sublane.rs
  - crates/velos-vehicle/src/social_force.rs
  - crates/velos-vehicle/src/mobil.rs
  - crates/velos-vehicle/tests/config_tests.rs
  - crates/velos-vehicle/tests/types_tests.rs
  - data/hcmc/vehicle_params.toml
autonomous: true
requirements: [TUN-01, TUN-03]

must_haves:
  truths:
    - "All vehicle behavior parameters load from data/hcmc/vehicle_params.toml"
    - "Each vehicle type has HCMC-calibrated defaults (truck v0=35 km/h not 90 km/h, car v0=35 km/h not 50 km/h)"
    - "default_idm_params() and default_mobil_params() return values from config, not hardcoded literature values"
    - "Config validation rejects out-of-range parameters with descriptive errors"
  artifacts:
    - path: "data/hcmc/vehicle_params.toml"
      provides: "HCMC-calibrated per-vehicle-type parameter defaults"
      contains: "[motorbike]"
    - path: "crates/velos-vehicle/src/config.rs"
      provides: "VehicleConfig struct with TOML deserialization and validation"
      exports: ["VehicleConfig", "VehicleTypeParams", "PedestrianParams", "load_vehicle_config"]
      min_lines: 80
    - path: "crates/velos-vehicle/tests/config_tests.rs"
      provides: "Config loading, validation, and default fallback tests"
      min_lines: 40
  key_links:
    - from: "crates/velos-vehicle/src/config.rs"
      to: "data/hcmc/vehicle_params.toml"
      via: "toml::from_str deserialization"
      pattern: "toml::from_str"
    - from: "crates/velos-vehicle/src/types.rs"
      to: "crates/velos-vehicle/src/config.rs"
      via: "factory functions delegate to config defaults"
      pattern: "VehicleTypeParams|VehicleConfig"
---

<objective>
Create the TOML config infrastructure for vehicle behavior parameters and migrate all hardcoded defaults to HCMC-calibrated values.

Purpose: Eliminate hardcoded vehicle parameters scattered across types.rs, krauss.rs, sublane.rs, and social_force.rs. Establish a single source of truth (vehicle_params.toml) with HCMC-calibrated defaults. This is the foundation for both GPU parameter unification (Plan 02) and behavioral rules (Plan 03).

Output: VehicleConfig module, TOML config file, migrated factory functions, updated tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tuning-vehicle-behavior-to-more-realistic-in-hcm/08-CONTEXT.md
@.planning/phases/08-tuning-vehicle-behavior-to-more-realistic-in-hcm/08-RESEARCH.md

@crates/velos-vehicle/src/types.rs
@crates/velos-vehicle/src/krauss.rs
@crates/velos-vehicle/src/sublane.rs
@crates/velos-vehicle/src/idm.rs
@crates/velos-vehicle/src/mobil.rs
@crates/velos-vehicle/src/social_force.rs
@crates/velos-vehicle/Cargo.toml
@crates/velos-meso/src/zone_config.rs

<interfaces>
<!-- Existing structs that will be read from config instead of hardcoded -->

From crates/velos-vehicle/src/idm.rs:
```rust
pub struct IdmParams {
    pub v0: f64,
    pub s0: f64,
    pub t_headway: f64,
    pub a: f64,
    pub b: f64,
    pub delta: f64,
}
```

From crates/velos-vehicle/src/krauss.rs:
```rust
pub struct KraussParams {
    pub accel: f64,
    pub decel: f64,
    pub sigma: f64,
    pub tau: f64,
    pub max_speed: f64,
    pub min_gap: f64,
}
```

From crates/velos-vehicle/src/sublane.rs:
```rust
pub struct SublaneParams {
    pub min_filter_gap: f64,
    pub max_lateral_speed: f64,
    pub half_width: f64,
    pub swarm_lateral_speed: f64,
}
```

From crates/velos-vehicle/src/mobil.rs (MobilParams):
```rust
pub struct MobilParams {
    pub politeness: f64,
    pub threshold: f64,
    pub safe_decel: f64,
    pub right_bias: f64,
}
```

From crates/velos-vehicle/src/types.rs:
```rust
pub enum VehicleType {
    Motorbike, Car, Bus, Bicycle, Truck, Emergency, Pedestrian,
}
pub fn default_idm_params(vehicle_type: VehicleType) -> IdmParams;
pub fn default_mobil_params() -> MobilParams;
```

Existing TOML loading pattern from crates/velos-meso/src/zone_config.rs:
```rust
pub fn load_from_toml_str(toml_str: &str) -> Result<Self, Error> {
    toml::from_str(toml_str).map_err(|e| Error::ConfigParse(e.to_string()))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VehicleConfig module + TOML config file with HCMC defaults</name>
  <files>
    crates/velos-vehicle/Cargo.toml,
    crates/velos-vehicle/src/lib.rs,
    crates/velos-vehicle/src/config.rs,
    data/hcmc/vehicle_params.toml,
    crates/velos-vehicle/tests/config_tests.rs
  </files>
  <behavior>
    - Test: TOML file parses into VehicleConfig without error
    - Test: Each vehicle type section has all required fields (v0, s0, t_headway, a, b, delta, krauss_sigma, politeness, gap_acceptance_ttc)
    - Test: Motorbike v0 is in range 9.7-12.5 m/s (35-45 km/h)
    - Test: Car v0 is in range 8.3-11.1 m/s (30-40 km/h)
    - Test: Truck v0 is in range 8.3-11.1 m/s (30-40 km/h), NOT 25.0 m/s
    - Test: Bus v0 is in range 6.9-9.7 m/s (25-35 km/h)
    - Test: Bicycle v0 is in range 3.3-5.0 m/s (12-18 km/h)
    - Test: Pedestrian desired_speed in range 1.0-1.4 m/s
    - Test: validate() rejects v0 <= 0 with descriptive error
    - Test: VehicleTypeParams converts to IdmParams, KraussParams, MobilParams, SublaneParams correctly
  </behavior>
  <action>
    1. Add `toml.workspace = true` and `serde = { workspace = true, features = ["derive"] }` to velos-vehicle/Cargo.toml dependencies.

    2. Create `crates/velos-vehicle/src/config.rs`:
       - `VehicleConfig` struct with fields: motorbike, car, bus, truck, bicycle, emergency, pedestrian
       - `VehicleTypeParams` struct (serde Deserialize): v0, s0, t_headway, a, b, delta, krauss_accel, krauss_decel, krauss_sigma, krauss_tau, krauss_max_speed, krauss_min_gap, politeness, threshold, safe_decel, right_bias, gap_acceptance_ttc, min_filter_gap (Option), max_lateral_speed (Option), half_width (Option), swarm_lateral_speed (Option)
       - `PedestrianParams` struct: desired_speed, personal_space, jaywalking_rate_arterial, jaywalking_rate_local, gap_acceptance_ttc
       - `load_vehicle_config(path: &str) -> Result<VehicleConfig, Error>` using `std::fs::read_to_string` + `toml::from_str`
       - `load_vehicle_config_from_str(toml_str: &str) -> Result<VehicleConfig, Error>` (for tests)
       - `VehicleConfig::validate(&self) -> Result<(), Vec<String>>` checking all ranges from CONTEXT.md
       - Conversion methods: `VehicleTypeParams::to_idm_params() -> IdmParams`, `to_krauss_params() -> KraussParams`, `to_mobil_params() -> MobilParams`, `to_sublane_params() -> Option<SublaneParams>` (only if min_filter_gap is Some)
       - `VehicleConfig::default()` returning hardcoded HCMC defaults matching the TOML file (fallback)
       - `VehicleConfig::for_vehicle_type(&self, vt: VehicleType) -> &VehicleTypeParams`

    3. Create `data/hcmc/vehicle_params.toml` with HCMC-calibrated defaults per CONTEXT.md ranges:
       - [motorbike]: v0=11.1 (40 km/h), s0=1.0, t_headway=0.8, a=2.0, b=3.0, krauss_sigma=0.3, politeness=0.1, gap_acceptance_ttc=1.0, min_filter_gap=0.5, max_lateral_speed=1.2
       - [car]: v0=9.7 (35 km/h), s0=2.0, t_headway=1.5, a=1.0, b=2.0, krauss_sigma=0.5, politeness=0.3, gap_acceptance_ttc=1.5
       - [bus]: v0=8.3 (30 km/h), s0=3.0, t_headway=1.5, a=1.0, b=2.5, krauss_sigma=0.4, politeness=0.5, gap_acceptance_ttc=1.8
       - [truck]: v0=9.7 (35 km/h, NOT 25.0/90 km/h), s0=4.0, t_headway=2.0, a=1.0, b=2.5, krauss_sigma=0.4, politeness=0.4, gap_acceptance_ttc=2.0
       - [bicycle]: v0=4.17 (15 km/h), s0=1.5, t_headway=1.0, a=1.0, b=3.0, krauss_sigma=0.3, politeness=0.2, gap_acceptance_ttc=1.2, min_filter_gap=0.5, max_lateral_speed=0.8
       - [emergency]: v0=16.7 (60 km/h), s0=2.0, t_headway=1.2, a=2.0, b=3.5, krauss_sigma=0.2, politeness=0.0, gap_acceptance_ttc=0.5
       - [pedestrian]: desired_speed=1.2, personal_space=0.5, jaywalking_rate_arterial=0.1, jaywalking_rate_local=0.3, gap_acceptance_ttc=2.0
       - All vehicle type sections also include: delta=4.0, krauss_accel, krauss_decel, krauss_tau=1.0, krauss_max_speed (matching v0), krauss_min_gap (matching s0), threshold=0.2, safe_decel=-4.0, right_bias=0.1

    4. Add `pub mod config;` to lib.rs.

    5. Write config_tests.rs with tests for all behaviors listed above.
  </action>
  <verify>
    <automated>cargo test -p velos-vehicle config -- --nocapture</automated>
  </verify>
  <done>VehicleConfig module loads TOML, validates ranges, converts to existing param structs. TOML file has HCMC-calibrated defaults for all 7 vehicle types. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate factory functions to config-backed defaults + update existing tests</name>
  <files>
    crates/velos-vehicle/src/types.rs,
    crates/velos-vehicle/src/krauss.rs,
    crates/velos-vehicle/src/sublane.rs,
    crates/velos-vehicle/src/mobil.rs,
    crates/velos-vehicle/tests/types_tests.rs,
    crates/velos-vehicle/tests/sublane_tests.rs,
    crates/velos-vehicle/tests/krauss_tests.rs,
    crates/velos-vehicle/tests/mobil_tests.rs
  </files>
  <action>
    1. Update `types.rs`:
       - Change `default_idm_params(vehicle_type)` to return HCMC-calibrated values matching `VehicleConfig::default()`. The function keeps its signature for backward compatibility but now returns HCMC values instead of literature values.
       - Key changes: Car v0: 13.9 -> 9.7, Car a: 1.0 stays, Car b: 2.0 stays. Truck v0: 25.0 -> 9.7. Bus v0: 11.1 -> 8.3. Motorbike t_headway: 1.0 -> 0.8.
       - Add `default_idm_params_from_config(vt: VehicleType, config: &VehicleConfig) -> IdmParams` that reads from config.
       - Change `default_mobil_params()` to return HCMC values (politeness 0.3 stays, but add per-type variant).
       - Add `default_mobil_params_for_type(vt: VehicleType, config: &VehicleConfig) -> MobilParams`.

    2. Update `krauss.rs`:
       - Change `KraussParams::sumo_default()` to return HCMC car defaults (max_speed: 13.89 -> 9.7).
       - Add `KraussParams::from_config(params: &VehicleTypeParams) -> Self`.

    3. Update `sublane.rs`:
       - Change `SublaneParams::default()` to return HCMC motorbike defaults (min_filter_gap: 0.6 -> 0.5, max_lateral_speed: 1.0 -> 1.2).
       - Add `SublaneParams::from_config(params: &VehicleTypeParams) -> Option<Self>` (None if no sublane fields).

    4. Update `mobil.rs` if it has a default function -- add `MobilParams::from_config(params: &VehicleTypeParams) -> Self`.

    5. Update all affected test files:
       - `types_tests.rs`: Update assertions to match new HCMC defaults (truck v0=9.7, car v0=9.7, etc.)
       - `sublane_tests.rs`: Update `default_params_match_context_md` test for new defaults (min_filter_gap=0.5, max_lateral_speed=1.2)
       - `krauss_tests.rs`: Update if it asserts on `sumo_default()` values
       - `mobil_tests.rs`: Update if it asserts on default politeness

    6. Ensure `cargo test -p velos-vehicle` passes with all updated defaults.
  </action>
  <verify>
    <automated>cargo test -p velos-vehicle -- --nocapture</automated>
  </verify>
  <done>All factory functions return HCMC-calibrated defaults. from_config() methods exist on all param structs. All existing tests updated and passing. No hardcoded non-HCMC parameter values remain in types.rs factory functions.</done>
</task>

</tasks>

<verification>
```bash
# All velos-vehicle tests pass
cargo test -p velos-vehicle -- --nocapture

# Clippy clean
cargo clippy -p velos-vehicle -- -D warnings

# Config file parses correctly
cargo test -p velos-vehicle config -- --nocapture
```
</verification>

<success_criteria>
1. `data/hcmc/vehicle_params.toml` exists with HCMC-calibrated per-vehicle-type parameters
2. `VehicleConfig` struct loads from TOML and validates parameter ranges
3. All factory functions (`default_idm_params`, `default_mobil_params`, `KraussParams::sumo_default`, `SublaneParams::default`) return HCMC-calibrated values
4. `from_config()` methods on all param structs enable config-driven parameter loading
5. Truck v0 is 9.7 m/s (35 km/h), not 25.0 m/s (90 km/h)
6. `cargo test -p velos-vehicle` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/08-tuning-vehicle-behavior-to-more-realistic-in-hcm/08-01-SUMMARY.md`
</output>
