---
phase: 06-agent-models-signal-control
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - crates/velos-signal/src/actuated.rs
  - crates/velos-signal/src/adaptive.rs
  - crates/velos-signal/src/detector.rs
  - crates/velos-signal/src/lib.rs
  - crates/velos-signal/tests/actuated_tests.rs
  - crates/velos-signal/tests/adaptive_tests.rs
  - crates/velos-signal/tests/detector_tests.rs
autonomous: true
requirements: [SIG-01, SIG-02]

must_haves:
  truths:
    - "Actuated controller extends green when detector sees vehicles, gaps out after 3s with no detection"
    - "Actuated controller respects min_green=7s and max_green=60s bounds"
    - "Adaptive controller redistributes green time proportional to queue length each cycle"
    - "Loop detector fires when agent crosses sensor point on approach lane"
    - "Both controllers implement the same SignalController trait as FixedTimeController"
  artifacts:
    - path: "crates/velos-signal/src/actuated.rs"
      provides: "ActuatedController with gap-out state machine"
      exports: ["ActuatedController"]
    - path: "crates/velos-signal/src/adaptive.rs"
      provides: "AdaptiveController with queue-proportional timing"
      exports: ["AdaptiveController"]
    - path: "crates/velos-signal/src/detector.rs"
      provides: "LoopDetector virtual point sensor"
      exports: ["LoopDetector", "DetectorReading"]
  key_links:
    - from: "crates/velos-signal/src/actuated.rs"
      to: "crates/velos-signal/src/detector.rs"
      via: "ActuatedController reads DetectorReading to decide gap-out"
      pattern: "DetectorReading"
    - from: "crates/velos-signal/src/adaptive.rs"
      to: "crates/velos-signal/src/actuated.rs"
      via: "Both implement SignalController trait"
      pattern: "impl SignalController"
---

<objective>
Implement actuated and adaptive signal controllers alongside the existing fixed-time controller. Actuated uses loop detector gap-out logic; adaptive redistributes green time based on queue lengths.

Purpose: Signals that respond to traffic demand are essential for SUMO replacement -- fixed-time-only is unrealistic for modern intersections.
Output: ActuatedController, AdaptiveController, LoopDetector, SignalController trait.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-models-signal-control/06-CONTEXT.md
@.planning/phases/06-agent-models-signal-control/06-RESEARCH.md

<interfaces>
From crates/velos-signal/src/plan.rs:
```rust
pub enum PhaseState { Green, Amber, Red }
pub struct SignalPhase { pub green_duration: f64, pub amber_duration: f64, pub approaches: Vec<usize> }
pub struct SignalPlan { pub phases: Vec<SignalPhase>, pub cycle_time: f64 }
```

From crates/velos-signal/src/controller.rs:
```rust
pub struct FixedTimeController {
    plan: SignalPlan,
    elapsed: f64,
    num_approaches: usize,
}
impl FixedTimeController {
    pub fn new(plan: SignalPlan, num_approaches: usize) -> Self;
    pub fn tick(&mut self, dt: f64);
    pub fn get_phase_state(&self, approach_index: usize) -> PhaseState;
    pub fn reset(&mut self);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignalController trait + LoopDetector + ActuatedController</name>
  <files>
    crates/velos-signal/src/detector.rs,
    crates/velos-signal/src/actuated.rs,
    crates/velos-signal/src/lib.rs,
    crates/velos-signal/tests/detector_tests.rs,
    crates/velos-signal/tests/actuated_tests.rs
  </files>
  <behavior>
    - SignalController trait with tick, get_phase_state, reset methods
    - FixedTimeController implements SignalController (add impl, do NOT change its existing API)
    - LoopDetector: edge_id, offset_m, last_triggered flag
    - LoopDetector::check(agent_prev_pos, agent_cur_pos) returns true if agent crossed detector point
    - ActuatedController with gap-out timer per phase
    - Actuated: stays green while detector fires; gaps out after 3s silence
    - Actuated: min green 7s -- won't transition even if gap-out before 7s
    - Actuated: max green 60s -- always transitions at 60s regardless of detector
    - Actuated: after transition, next phase starts fresh with 0 gap timer
  </behavior>
  <action>
    1. Create crates/velos-signal/src/detector.rs:
       - `LoopDetector` struct: edge_id: u32, offset_m: f64
       - `DetectorReading` struct: detector_index: usize, triggered: bool
       - `LoopDetector::check(prev_pos: f64, cur_pos: f64) -> bool` -- returns true if prev_pos < offset_m <= cur_pos (agent crossed sensor point going forward)
       - Virtual point sensor per RESEARCH.md recommendation

    2. Define `SignalController` trait in velos-signal/src/lib.rs (or a new traits.rs):
       ```rust
       pub trait SignalController {
           fn tick(&mut self, dt: f64, detectors: &[DetectorReading]);
           fn get_phase_state(&self, approach_index: usize) -> PhaseState;
           fn reset(&mut self);
       }
       ```
       - Implement SignalController for FixedTimeController (tick ignores detectors, delegates to existing self.tick(dt))

    3. Create crates/velos-signal/src/actuated.rs:
       - `ActuatedController` struct per RESEARCH.md code example: plan, elapsed, current_phase_idx, phase_active_time, gap_timer, min_green (7.0), max_green (60.0), gap_threshold (3.0), num_approaches
       - `new(plan: SignalPlan, num_approaches: usize) -> Self` with HCMC defaults
       - `new_with_params(plan, num_approaches, min_green, max_green, gap_threshold)` for testing
       - Tick logic: advance timers, reset gap_timer when detector on current phase approach fires, transition when phase_active_time >= max_green OR (phase_active_time >= min_green AND gap_timer >= gap_threshold)
       - Implement SignalController trait

    4. Add modules to lib.rs, re-export key types

    5. Write tests:
       - detector_tests.rs: crossing detection, no-cross detection, backwards movement
       - actuated_tests.rs: min green holds, gap-out triggers transition, max green forces transition, detector resets gap timer
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-signal 2>&1 | tail -5</automated>
  </verify>
  <done>SignalController trait defined. LoopDetector detects agent crossings. ActuatedController implements gap-out logic with min/max green bounds. All signal tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdaptiveController with queue-proportional green redistribution</name>
  <files>
    crates/velos-signal/src/adaptive.rs,
    crates/velos-signal/src/lib.rs,
    crates/velos-signal/tests/adaptive_tests.rs
  </files>
  <behavior>
    - AdaptiveController redistributes green durations proportional to queue lengths at end of each cycle
    - With queue_lengths [10, 30, 20, 40] and total_green=80s, phases get [8, 24, 16, 32]s green
    - Minimum green per phase: 7s (never goes below regardless of queue proportion)
    - With all queues equal, green time distributed equally
    - With all queues zero, keeps previous timing unchanged
    - Implements SignalController trait
    - During a cycle, behaves like FixedTimeController (phase transitions at elapsed boundaries)
  </behavior>
  <action>
    1. Create crates/velos-signal/src/adaptive.rs:
       - `AdaptiveController` struct: plan (mutable -- green durations change), elapsed, current_phase_idx, num_approaches, min_green_per_phase (7.0), queue_lengths (Vec<u32> per approach)
       - `new(plan: SignalPlan, num_approaches: usize) -> Self`
       - `update_queue_lengths(&mut self, queue_lengths: &[u32])` -- called externally once per cycle
       - At cycle end (elapsed wraps), call internal `redistribute_green()`:
         - Total available green = sum of all phase green durations
         - For each phase, compute proportional green based on sum of queue_lengths for that phase's approaches
         - Enforce min_green per phase
         - After enforcement, distribute remaining green proportionally among phases above minimum
         - Recompute plan.cycle_time
       - `get_phase_state` works like FixedTimeController (walk phases by elapsed time)
       - Implement SignalController trait

    2. Add `pub mod adaptive;` to lib.rs, re-export

    3. Write tests in adaptive_tests.rs:
       - Proportional redistribution with known queue lengths
       - Minimum green enforcement
       - Equal queues -> equal distribution
       - Zero queues -> unchanged timing
       - Full cycle behavior (tick through entire cycle, verify phase transitions)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-signal --lib adaptive 2>&1 | tail -5</automated>
  </verify>
  <done>AdaptiveController redistributes green proportionally to queue lengths each cycle. Min green enforced. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p velos-signal` -- all signal tests pass (fixed, actuated, adaptive, detector)
- `cargo test --workspace` -- no regressions
- `cargo clippy -p velos-signal` -- zero warnings
</verification>

<success_criteria>
- SignalController trait unifies FixedTime, Actuated, Adaptive controllers
- ActuatedController: gap-out at 3s, min green 7s, max green 60s
- AdaptiveController: queue-proportional green redistribution per cycle
- LoopDetector: virtual point sensor detects agent crossings
- All controllers produce correct PhaseState for any approach at any time
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-models-signal-control/06-03-SUMMARY.md`
</output>
