---
phase: 06-agent-models-signal-control
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - crates/velos-vehicle/src/bus.rs
  - crates/velos-vehicle/src/lib.rs
  - crates/velos-vehicle/tests/bus_tests.rs
  - crates/velos-demand/src/gtfs.rs
  - crates/velos-demand/src/lib.rs
  - crates/velos-demand/src/spawner.rs
  - crates/velos-demand/Cargo.toml
  - crates/velos-demand/tests/gtfs_tests.rs
  - data/gtfs/test_fixture/
autonomous: true
requirements: [AGT-01, AGT-02]

must_haves:
  truths:
    - "Bus dwell time computed as 5s + 0.5s/boarding + 0.67s/alighting, capped at 60s"
    - "GTFS routes/stops/stop_times parsed into BusRoute structs with edge positions"
    - "Bus stops are ECS-attachable components with edge_id, offset, and capacity"
    - "Buses use IDM car-following with schedule-aware desired speed"
  artifacts:
    - path: "crates/velos-vehicle/src/bus.rs"
      provides: "BusDwellModel, BusStop, BusState, bus dwell computation"
      exports: ["BusDwellModel", "BusStop", "BusState"]
    - path: "crates/velos-demand/src/gtfs.rs"
      provides: "GTFS import producing BusRoute/BusSchedule structs"
      exports: ["load_gtfs", "BusRoute", "BusSchedule", "GtfsStop"]
  key_links:
    - from: "crates/velos-demand/src/gtfs.rs"
      to: "crates/velos-vehicle/src/bus.rs"
      via: "GTFS stop positions map to BusStop edge+offset"
      pattern: "BusStop"
    - from: "crates/velos-vehicle/src/bus.rs"
      to: "crates/velos-gpu/shaders/wave_front.wgsl"
      via: "Bus dwell state sets FLAG_BUS_DWELLING in GpuAgentState.flags"
      pattern: "FLAG_BUS_DWELLING"
---

<objective>
Implement bus agents with empirical dwell time model and GTFS import for HCMC bus routes. Buses stop at designated locations, dwell for computed boarding/alighting time, and resume on schedule.

Purpose: Bus transit is a core agent type for SUMO replacement. GTFS import enables loading real HCMC bus route data.
Output: BusDwellModel, BusStop ECS component, GTFS importer, bus spawning integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-models-signal-control/06-CONTEXT.md
@.planning/phases/06-agent-models-signal-control/06-RESEARCH.md
@.planning/phases/06-agent-models-signal-control/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

Expected from crates/velos-core/src/components.rs:
```rust
pub enum VehicleType { Motorbike, Car, Bus, Bicycle, Truck, Emergency, Pedestrian }

pub struct GpuAgentState {
    // ... existing fields ...
    pub vehicle_type: u32,   // 32..36  0=Motorbike,1=Car,2=Bus,...
    pub flags: u32,          // 36..40  bit0=at_bus_stop, bit1=emergency_active, bit2=yielding
}
```

From crates/velos-vehicle/src/idm.rs:
```rust
pub struct IdmParams { pub v0: f64, pub s0: f64, pub t_headway: f64, pub a: f64, pub b: f64, pub delta: f64 }
pub fn idm_acceleration(params: &IdmParams, v: f64, s: f64, delta_v: f64) -> f64;
```

From crates/velos-demand/src/spawner.rs:
```rust
pub enum SpawnVehicleType { Motorbike, Car, Pedestrian, Bus, Bicycle, Truck, Emergency }
pub struct SpawnRequest { pub origin: Zone, pub destination: Zone, pub vehicle_type: SpawnVehicleType }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bus dwell model and BusStop ECS component</name>
  <files>
    crates/velos-vehicle/src/bus.rs,
    crates/velos-vehicle/src/lib.rs,
    crates/velos-vehicle/tests/bus_tests.rs
  </files>
  <behavior>
    - BusDwellModel::compute_dwell(10 boarding, 5 alighting) == 5.0 + 5.0 + 3.35 = 13.35s
    - BusDwellModel::compute_dwell(100 boarding, 100 alighting) capped at 60.0s
    - BusDwellModel::compute_dwell(0, 0) == 5.0s (fixed door open/close)
    - BusStop has edge_id, offset_m, capacity, name fields
    - BusState tracks current_stop_index, dwell_remaining, is_dwelling, next_departure_time
    - BusState::should_stop returns true when agent position within 5m of next stop
    - BusState::begin_dwell sets dwell_remaining from BusDwellModel with random passenger counts
    - BusState::tick_dwell decrements timer and returns true when dwell complete
  </behavior>
  <action>
    1. Create crates/velos-vehicle/src/bus.rs:
       - `BusDwellModel` struct with fixed_dwell_s=5.0, per_boarding_s=0.5, per_alighting_s=0.67, max_dwell_s=60.0
       - `BusDwellModel::compute_dwell(boarding: u32, alighting: u32) -> f64`
       - `BusStop` struct: edge_id: u32, offset_m: f64, capacity: u16, name: String
       - `BusState` struct tracking active bus journey: stops (Vec of BusStop refs as indices), current_stop_index, dwell_remaining, is_dwelling
       - `BusState::should_stop(current_edge: u32, current_offset: f64, stops: &[BusStop]) -> bool` -- within 5m of next stop on same edge
       - `BusState::begin_dwell(model: &BusDwellModel, boarding: u32, alighting: u32)` -- sets is_dwelling=true and dwell_remaining
       - `BusState::tick_dwell(dt: f64) -> bool` -- returns true when dwell complete, sets is_dwelling=false
       - Note: passenger counts are stochastic (generated by caller with RNG), not determined by BusState

    2. Add `pub mod bus;` to velos-vehicle/src/lib.rs

    3. Write tests in bus_tests.rs covering all behavior items above
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-vehicle --lib bus 2>&1 | tail -5</automated>
  </verify>
  <done>BusDwellModel computes correct dwell times. BusStop and BusState structs support stop detection and dwell lifecycle. All bus tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GTFS import for HCMC bus routes</name>
  <files>
    crates/velos-demand/src/gtfs.rs,
    crates/velos-demand/src/lib.rs,
    crates/velos-demand/Cargo.toml,
    crates/velos-demand/tests/gtfs_tests.rs,
    data/gtfs/test_fixture/routes.txt,
    data/gtfs/test_fixture/stops.txt,
    data/gtfs/test_fixture/trips.txt,
    data/gtfs/test_fixture/stop_times.txt
  </files>
  <behavior>
    - load_gtfs("path/to/gtfs") parses routes.txt, stops.txt, trips.txt, stop_times.txt
    - BusRoute contains route_id, route_name, list of GtfsStop (stop_id, lat, lon, name)
    - BusSchedule contains trip_id, route_id, ordered stop_times with arrival/departure
    - load_gtfs returns Vec<BusRoute> and Vec<BusSchedule>
    - Missing or malformed rows produce warnings but don't fail the parse
    - Test fixture with 2 routes and 5 stops parses correctly
  </behavior>
  <action>
    1. Add `gtfs-structures = "0.47"` to crates/velos-demand/Cargo.toml

    2. Create crates/velos-demand/src/gtfs.rs:
       - `GtfsStop` struct: stop_id: String, name: String, lat: f64, lon: f64
       - `BusRoute` struct: route_id: String, route_name: String, stops: Vec<GtfsStop>
       - `BusSchedule` struct: trip_id: String, route_id: String, stop_times: Vec<StopTime>
       - `StopTime` struct: stop_id: String, arrival_s: u32 (seconds from midnight), departure_s: u32, stop_sequence: u32
       - `load_gtfs(path: &Path) -> Result<(Vec<BusRoute>, Vec<BusSchedule>), DemandError>`
       - Uses `gtfs_structures::Gtfs::new(path)` to parse
       - Extract only routes, stops, trips, stop_times (ignore fare, shape, calendar per RESEARCH.md)
       - Fallback: if gtfs-structures fails, provide `load_gtfs_csv(path: &Path)` that reads raw CSV files for manual route/stop data (HCMC GTFS availability uncertain)
       - Map stop lat/lon to GtfsStop; trip stop_times to BusSchedule

    3. Add `pub mod gtfs;` to velos-demand/src/lib.rs, add re-exports

    4. Create test fixture data/gtfs/test_fixture/ with minimal GTFS files:
       - routes.txt: 2 routes (route_01, route_02)
       - stops.txt: 5 stops with HCMC-area coordinates
       - trips.txt: 2 trips (one per route)
       - stop_times.txt: 3-4 stop_times per trip

    5. Write tests in gtfs_tests.rs:
       - Test load_gtfs with fixture directory
       - Assert correct route/stop/schedule counts
       - Assert stop coordinates are parsed correctly
       - Test CSV fallback path
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-demand --lib gtfs 2>&1 | tail -5</automated>
  </verify>
  <done>GTFS importer loads routes, stops, and schedules from standard GTFS files. Test fixture validates parsing. CSV fallback available for non-GTFS data.</done>
</task>

</tasks>

<verification>
- `cargo test -p velos-vehicle --lib bus` -- bus dwell model tests pass
- `cargo test -p velos-demand --lib gtfs` -- GTFS import tests pass
- `cargo test --workspace` -- no regressions
- `cargo clippy --workspace` -- zero warnings
</verification>

<success_criteria>
- Bus dwell formula: 5s + 0.5s/boarding + 0.67s/alighting, capped 60s, verified by unit tests
- BusStop ECS component with edge_id + offset for road network attachment
- GTFS import parses routes.txt, stops.txt, trips.txt, stop_times.txt into typed structs
- Test fixture with 2 routes / 5 stops validates the full parse pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-models-signal-control/06-02-SUMMARY.md`
</output>
