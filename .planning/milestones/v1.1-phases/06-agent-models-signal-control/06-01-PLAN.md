---
phase: 06-agent-models-signal-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/velos-core/src/components.rs
  - crates/velos-core/tests/components_tests.rs
  - crates/velos-vehicle/src/types.rs
  - crates/velos-vehicle/tests/types_tests.rs
  - crates/velos-gpu/src/compute.rs
  - crates/velos-gpu/src/sim.rs
  - crates/velos-gpu/src/sim_lifecycle.rs
  - crates/velos-gpu/src/sim_render.rs
  - crates/velos-gpu/shaders/wave_front.wgsl
  - crates/velos-gpu/shaders/fixed_point.wgsl
  - crates/velos-gpu/tests/wave_front_validation.rs
  - crates/velos-gpu/tests/gpu_physics.rs
  - crates/velos-gpu/tests/cf_model_switch.rs
  - crates/velos-demand/src/spawner.rs
autonomous: true
requirements: [AGT-03, AGT-07]

must_haves:
  truths:
    - "GpuAgentState is 40 bytes with vehicle_type and flags fields"
    - "VehicleType enum has Bus, Bicycle, Truck, Emergency variants in both velos-core and velos-vehicle"
    - "default_idm_params returns correct parameters for all 7 vehicle types"
    - "Bicycle agents use sublane model with IDM v0=4.17 m/s and 0.3m half-width"
    - "Truck agents use IDM with s0=4m, T=2.0s, a=1.0, v0=25 m/s"
    - "All existing GPU tests pass with the 40-byte struct"
  artifacts:
    - path: "crates/velos-core/src/components.rs"
      provides: "Extended GpuAgentState (40 bytes) and VehicleType enum"
      contains: "vehicle_type: u32"
    - path: "crates/velos-vehicle/src/types.rs"
      provides: "Extended VehicleType + default_idm_params for all types"
      contains: "VehicleType::Truck"
  key_links:
    - from: "crates/velos-core/src/components.rs"
      to: "crates/velos-gpu/shaders/wave_front.wgsl"
      via: "GpuAgentState struct layout must match WGSL AgentState struct"
      pattern: "vehicle_type.*u32"
    - from: "crates/velos-vehicle/src/types.rs"
      to: "crates/velos-gpu/src/sim_lifecycle.rs"
      via: "VehicleType variants used in spawn_single_agent"
      pattern: "VehicleType::(Bus|Bicycle|Truck|Emergency)"
---

<objective>
Expand GpuAgentState from 32 to 40 bytes, extend VehicleType enum with Bus/Bicycle/Truck/Emergency, and add IDM parameters for all new vehicle types. This is the foundation task -- every other Phase 6 plan depends on these changes.

Purpose: All subsequent agent types, signals, and GPU shader branching require the vehicle_type field in GpuAgentState and the extended VehicleType enum.
Output: 40-byte GpuAgentState, 7-variant VehicleType, default IDM params for all types, all existing tests green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-models-signal-control/06-CONTEXT.md
@.planning/phases/06-agent-models-signal-control/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From crates/velos-core/src/components.rs (CURRENT -- will be modified):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum VehicleType { Motorbike, Car, Pedestrian }

#[repr(C)]
#[derive(Copy, Clone, Debug, PartialEq, Eq, bytemuck::Pod, bytemuck::Zeroable)]
pub struct GpuAgentState {
    pub edge_id: u32,        // 0..4
    pub lane_idx: u32,       // 4..8
    pub position: i32,       // 8..12  Q16.16
    pub lateral: i32,        // 12..16 Q8.8 in i32
    pub speed: i32,          // 16..20 Q12.20
    pub acceleration: i32,   // 20..24 Q12.20
    pub cf_model: u32,       // 24..28
    pub rng_state: u32,      // 28..32
}
// Total: 32 bytes currently
```

From crates/velos-vehicle/src/types.rs (CURRENT -- will be modified):
```rust
pub enum VehicleType { Motorbike, Car, Pedestrian }
pub fn default_idm_params(vehicle_type: VehicleType) -> IdmParams { ... }
pub fn default_mobil_params() -> MobilParams { ... }
```

From crates/velos-demand/src/spawner.rs:
```rust
pub enum SpawnVehicleType { Motorbike, Car, Pedestrian }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand GpuAgentState to 40 bytes and extend VehicleType enum</name>
  <files>
    crates/velos-core/src/components.rs,
    crates/velos-core/tests/components_tests.rs,
    crates/velos-vehicle/src/types.rs,
    crates/velos-vehicle/tests/types_tests.rs
  </files>
  <behavior>
    - GpuAgentState size_of == 40 bytes
    - GpuAgentState has vehicle_type: u32 at byte offset 32
    - GpuAgentState has flags: u32 at byte offset 36
    - VehicleType enum in velos-core has 7 variants: Motorbike, Car, Bus, Bicycle, Truck, Emergency, Pedestrian
    - VehicleType enum in velos-vehicle matches velos-core (same 7 variants, same order)
    - default_idm_params(VehicleType::Bus) returns v0=11.1, s0=3.0, t_headway=1.5, a=1.0
    - default_idm_params(VehicleType::Bicycle) returns v0=4.17, s0=1.5, t_headway=1.0, a=1.0
    - default_idm_params(VehicleType::Truck) returns v0=25.0, s0=4.0, t_headway=2.0, a=1.0
    - default_idm_params(VehicleType::Emergency) returns v0=16.7, s0=2.0, t_headway=1.2, a=2.0
    - bytemuck::cast_slice on GpuAgentState array works without panic
  </behavior>
  <action>
    1. In velos-core/src/components.rs:
       - Add Bus, Bicycle, Truck, Emergency variants to VehicleType enum (in order: Motorbike, Car, Bus, Bicycle, Truck, Emergency, Pedestrian)
       - Add `vehicle_type: u32` and `flags: u32` fields to GpuAgentState after rng_state
       - Update doc comments: "40 bytes total" and document the new fields
       - vehicle_type mapping: 0=Motorbike, 1=Car, 2=Bus, 3=Bicycle, 4=Truck, 5=Emergency, 6=Pedestrian
       - flags bitfield: bit0=at_bus_stop, bit1=emergency_active, bit2=yielding (document in comments)

    2. In velos-vehicle/src/types.rs:
       - Add Bus, Bicycle, Truck, Emergency variants to VehicleType enum (same order as velos-core)
       - Extend default_idm_params match arms:
         - Bus: v0=11.1 (40km/h), s0=3.0, t_headway=1.5, a=1.0, b=2.5, delta=4.0
         - Bicycle: v0=4.17 (15km/h), s0=1.5, t_headway=1.0, a=1.0, b=3.0, delta=4.0
         - Truck: v0=25.0 (90km/h), s0=4.0, t_headway=2.0, a=1.0, b=2.5, delta=4.0
         - Emergency: v0=16.7 (60km/h), s0=2.0, t_headway=1.2, a=2.0, b=3.5, delta=4.0

    3. Write tests in components_tests.rs:
       - Assert size_of::<GpuAgentState>() == 40
       - Assert vehicle_type field offset == 32 (use bytemuck cast + manual offset check)
       - Assert bytemuck cast_slice roundtrip works
       - Assert all 7 VehicleType variants exist

    4. Write tests in types_tests.rs (new file):
       - Assert default_idm_params returns expected values for each of the 7 vehicle types
       - Assert bicycle v0 == 4.17 (15 km/h)
       - Assert truck s0 == 4.0, t_headway == 2.0
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-core -p velos-vehicle --lib 2>&1 | tail -5</automated>
  </verify>
  <done>GpuAgentState is 40 bytes. VehicleType has 7 variants in both crates. All IDM params correct for all types.</done>
</task>

<task type="auto">
  <name>Task 2: Update all GPU buffer code and shaders for 40-byte GpuAgentState</name>
  <files>
    crates/velos-gpu/src/compute.rs,
    crates/velos-gpu/src/sim.rs,
    crates/velos-gpu/src/sim_lifecycle.rs,
    crates/velos-gpu/src/sim_render.rs,
    crates/velos-gpu/shaders/wave_front.wgsl,
    crates/velos-gpu/tests/wave_front_validation.rs,
    crates/velos-gpu/tests/gpu_physics.rs,
    crates/velos-gpu/tests/cf_model_switch.rs,
    crates/velos-demand/src/spawner.rs
  </files>
  <action>
    CRITICAL: Grep for ALL references to GpuAgentState across the entire codebase and update every site. The struct is used in compute.rs (~13 refs), multi_gpu.rs (~4 refs), sim.rs (~4 refs), plus test files.

    1. Update wave_front.wgsl AgentState struct:
       - Add `vehicle_type: u32` and `flags: u32` after rng_state
       - Add vehicle type constants: `const VT_MOTORBIKE: u32 = 0u; const VT_CAR: u32 = 1u; const VT_BUS: u32 = 2u; const VT_BICYCLE: u32 = 3u; const VT_TRUCK: u32 = 4u; const VT_EMERGENCY: u32 = 5u; const VT_PEDESTRIAN: u32 = 6u;`
       - Add flag constants: `const FLAG_BUS_DWELLING: u32 = 1u; const FLAG_EMERGENCY_ACTIVE: u32 = 2u; const FLAG_YIELDING: u32 = 4u;`
       - No behavior changes yet -- just read new fields, no branching on them (that comes in later plans)

    2. Update compute.rs:
       - All buffer size calculations using size_of::<GpuAgentState>() will auto-adjust
       - Verify any manual stride constants are updated if they exist (search for "32" near buffer creation)

    3. Update sim_lifecycle.rs spawn_single_agent:
       - Add SpawnVehicleType::Bus, SpawnVehicleType::Bicycle, SpawnVehicleType::Truck, SpawnVehicleType::Emergency to the vtype match
       - Set vehicle_type field when creating GpuAgentState during GPU upload
       - Initialize flags to 0u32

    4. Update SpawnVehicleType in velos-demand/src/spawner.rs:
       - Add Bus, Bicycle, Truck, Emergency variants
       - DO NOT change the existing 80/15/5 spawn distribution yet (bus/bicycle/truck spawning comes in later plans)

    5. Update sim_render.rs:
       - Add color coding for new vehicle types: Bus=yellow, Bicycle=cyan, Truck=brown, Emergency=red

    6. Fix ALL existing test files that create GpuAgentState instances:
       - Add `vehicle_type: 0, flags: 0` to every GpuAgentState literal
       - Run full test suite to verify nothing is broken

    7. Run: `cargo test --workspace` to verify ALL tests pass
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>All GPU buffer code uses 40-byte GpuAgentState. WGSL shader has vehicle_type/flags fields. All workspace tests pass. New vehicle type spawning and color coding works.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` -- all existing tests pass
- `cargo test -p velos-core -- components` -- GpuAgentState 40 bytes, VehicleType 7 variants
- `cargo test -p velos-vehicle -- types` -- IDM params correct for all vehicle types
- `cargo clippy --workspace` -- zero warnings
</verification>

<success_criteria>
- GpuAgentState is exactly 40 bytes with vehicle_type and flags fields
- VehicleType has 7 variants in both velos-core and velos-vehicle
- default_idm_params covers all 7 types with correct parameter values
- All existing GPU tests pass without regression
- WGSL shader struct matches Rust struct layout
- SpawnVehicleType extended with 4 new variants
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-models-signal-control/06-01-SUMMARY.md`
</output>
