---
phase: 15-file-size-reduction-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/velos-gpu/src/sim.rs
  - crates/velos-gpu/src/sim_signals.rs
  - crates/velos-gpu/src/sim_vehicles.rs
  - crates/velos-gpu/src/compute.rs
  - crates/velos-gpu/src/compute_wave_front.rs
  - crates/velos-gpu/src/lib.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "sim.rs is under 700 lines with no behavioral changes"
    - "compute.rs is under 700 lines with no behavioral changes"
    - "All existing velos-gpu tests pass after extraction"
    - "Clippy produces no warnings after extraction"
  artifacts:
    - path: "crates/velos-gpu/src/sim_signals.rs"
      provides: "Signal-related SimWorld methods extracted from sim.rs"
      contains: "step_signals_with_detectors"
    - path: "crates/velos-gpu/src/sim_vehicles.rs"
      provides: "GPU vehicle step method extracted from sim.rs"
      contains: "step_vehicles_gpu"
    - path: "crates/velos-gpu/src/compute_wave_front.rs"
      provides: "Wave-front dispatch methods extracted from compute.rs"
      contains: "upload_wave_front_data"
  key_links:
    - from: "crates/velos-gpu/src/lib.rs"
      to: "sim_signals, sim_vehicles, compute_wave_front"
      via: "mod declarations"
      pattern: "mod sim_signals|mod sim_vehicles|mod compute_wave_front"
    - from: "crates/velos-gpu/src/sim_signals.rs"
      to: "crates/velos-gpu/src/sim.rs"
      via: "impl SimWorld block extending struct defined in sim.rs"
      pattern: "impl SimWorld"
---

<objective>
Split sim.rs (948 lines) and compute.rs (1119 lines) below the 700-line project convention by extracting methods into focused submodules.

Purpose: Enforce project file size convention (<700 lines) without any behavioral changes -- pure refactoring.
Output: 3 new files (sim_signals.rs, sim_vehicles.rs, compute_wave_front.rs), 3 modified files (sim.rs, compute.rs, lib.rs)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-file-size-reduction-housekeeping/15-RESEARCH.md
@crates/velos-gpu/src/lib.rs
@crates/velos-gpu/src/sim.rs
@crates/velos-gpu/src/compute.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract sim.rs signal and vehicle methods into submodules</name>
  <files>crates/velos-gpu/src/sim.rs, crates/velos-gpu/src/sim_signals.rs, crates/velos-gpu/src/sim_vehicles.rs, crates/velos-gpu/src/lib.rs</files>
  <action>
Extract two groups of methods from sim.rs into new files, following the established sim_*.rs pattern (e.g., sim_bus.rs, sim_helpers.rs):

1. Create `sim_signals.rs` containing these methods cut from sim.rs (lines ~531-682):
   - `step_signals_with_detectors(&mut self, dt: f64, detector_readings: &[(NodeIndex, Vec<DetectorReading>)])`
   - `update_loop_detectors(&mut self, dt: f64) -> Vec<(NodeIndex, Vec<DetectorReading>)>`
   - `step_signal_priority(&mut self)`
   - Add module doc comment: `//! Signal-related SimWorld methods: detector updates, signal stepping, priority.`
   - Add all necessary `use` imports (copy from sim.rs, only what these methods need)
   - Use `use crate::sim::SimWorld;` if needed, or bare `impl SimWorld` if the crate pattern allows it (check sim_bus.rs for the exact import pattern used)

2. Create `sim_vehicles.rs` containing this method cut from sim.rs (lines ~684-812):
   - `step_vehicles_gpu(&mut self, dt: f32)`
   - Add module doc comment: `//! GPU vehicle physics step for SimWorld.`
   - Add necessary imports

3. Add to lib.rs alongside existing sim_* modules:
   ```rust
   mod sim_signals;
   mod sim_vehicles;
   ```

4. Remove the extracted method bodies from sim.rs (keep only the remaining: imports, types, struct, constructors, tick_gpu, tick, tests).

5. Verify sim.rs is under 700 lines with `wc -l`. Expected: ~665 lines.

IMPORTANT: Do NOT change any method signatures, visibility modifiers, or logic. This is a pure move operation. Preserve all `pub(crate)` and `pub` visibility exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-gpu --lib 2>&1 | tail -5 && cargo clippy -p velos-gpu -- -D warnings 2>&1 | tail -5 && echo "sim.rs lines:" && wc -l crates/velos-gpu/src/sim.rs</automated>
  </verify>
  <done>sim.rs is under 700 lines. sim_signals.rs and sim_vehicles.rs exist with the extracted methods. All tests pass. Clippy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract compute.rs wave-front methods into submodule</name>
  <files>crates/velos-gpu/src/compute.rs, crates/velos-gpu/src/compute_wave_front.rs, crates/velos-gpu/src/lib.rs</files>
  <action>
Extract wave-front related methods from compute.rs into a new file:

1. Create `compute_wave_front.rs` containing these items cut from compute.rs:
   - `upload_wave_front_data(...)` method on ComputeDispatcher
   - `dispatch_wave_front(...)` method on ComputeDispatcher
   - `readback_wave_front_agents(...)` method on ComputeDispatcher
   - Free functions: `sort_agents_by_lane(...)`, `compute_agent_flags(...)`, `bgl_entry(...)`
   - Add module doc comment: `//! Wave-front upload, dispatch, readback, and helper functions for ComputeDispatcher.`
   - Add necessary imports (check what types these functions reference)

2. Add to lib.rs:
   ```rust
   mod compute_wave_front;
   ```

3. Remove extracted items from compute.rs. Keep: GpuVehicleParams, param structs, ComputeDispatcher struct definition + new() + remaining methods + tests.

4. If `compute_agent_flags` or `bgl_entry` are used outside compute.rs (check with grep), ensure they remain accessible. `compute_agent_flags` is `pub` -- it may need re-export from compute.rs: `pub use compute_wave_front::compute_agent_flags;`. Similarly for `bgl_entry` if it's `pub(crate)` or `pub`.

5. Check if compute.rs is under 700 lines with `wc -l`. If tests push it over 700, move the `#[cfg(test)] mod tests { ... }` block into a separate `compute_tests.rs` file and add `#[cfg(test)] mod compute_tests;` in either compute.rs or lib.rs.

IMPORTANT: Do NOT change any function signatures, visibility, or logic. Pure move operation. Preserve all re-exports in lib.rs (e.g., `pub use compute::*` or specific items).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/velos && cargo test -p velos-gpu --lib 2>&1 | tail -5 && cargo clippy -p velos-gpu -- -D warnings 2>&1 | tail -5 && echo "compute.rs lines:" && wc -l crates/velos-gpu/src/compute.rs</automated>
  </verify>
  <done>compute.rs is under 700 lines. compute_wave_front.rs exists with extracted wave-front methods. All tests pass. Clippy clean. Any re-exports preserved.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/tindang/workspaces/tind-repo/velos
# All tests pass
cargo test --workspace 2>&1 | tail -10
# Both files under 700 lines
wc -l crates/velos-gpu/src/sim.rs crates/velos-gpu/src/compute.rs
# New files exist
ls -la crates/velos-gpu/src/sim_signals.rs crates/velos-gpu/src/sim_vehicles.rs crates/velos-gpu/src/compute_wave_front.rs
# Clippy clean
cargo clippy -p velos-gpu -- -D warnings
```
</verification>

<success_criteria>
- sim.rs < 700 lines (down from 948)
- compute.rs < 700 lines (down from 1119)
- 3 new submodule files created
- All workspace tests pass
- Clippy produces no warnings
- No behavioral changes (pure refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-size-reduction-housekeeping/15-01-SUMMARY.md`
</output>
